%section#content_body
  - if params[:query]
    .page_title_left
      =_("Suchergebnisse")
      #term
        = "für “#{params[:query]}”"
    .clear 
  = render :partial => "templates"
  #results

:javascript
  $(document).ready(function () {
    display_results(#{@json});
  });
